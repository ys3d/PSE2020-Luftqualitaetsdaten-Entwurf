package de.visaq.controller;

import jsweet.lang.JSON;

/**
 * Encapsulates the control over Cookie objects.
 */
public class CookieController {
	
	/**
	 * Sends the user data to the backend. There the important data is saved
	 * @param json The send data
	 */
	public synchronized void store(JSON json) {
		
	}

}
